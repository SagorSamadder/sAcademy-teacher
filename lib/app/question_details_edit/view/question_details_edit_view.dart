import 'package:flutter/material.dart';

class QuestionDetailsEdit extends StatelessWidget {
  const QuestionDetailsEdit({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text("Edit Question"),
      ),
    );
  }
}
