import 'package:flutter/material.dart';

Widget buildRapportsSection() {
  // Widget for Rapports section
  // This should contain the content for the "Rapports" destination
  return Container(
      color: Colors.green,
      child: const Center(
        child: Text("report section"),
      ));
}
